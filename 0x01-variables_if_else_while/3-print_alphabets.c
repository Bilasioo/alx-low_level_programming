#include <stdio.h>
/**
 * main - main function
 *
 * Return: always 0
 */

int main(void)
{
char k;

for (k = 'A'; k <= 'Z'; k++)K;
putchar(k);
putchar('\n');
return (0);
}
for (k = 'a' ; k <= 'z' ; k++)
putchar(k);
